## Resubmission

This is a resubmission. In this version I have:

* Simplified the `concordance()` examples so that they take less time to avoid note in debian check.

## R CMD check results

0 errors | 0 warnings | 0 notes

## revdepcheck results

There are currently no downstream dependencies for this package.
